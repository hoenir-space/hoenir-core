/**
 * This is a wrapper for the dependency [uuid](https://github.com/uuidjs/uuid).
 * @author Golos <golos@hoenir.space>
 */

import { v4 as uuid } from 'uuid'

const UUIDGenerator = uuid

export default UUIDGenerator
